import { FileSystemElement } from './filesystemelement';

describe('Filesystemelement', () => {
  it('should create an instance', () => {
//    expect(new FileSystemElement()).toBeTruthy();
  });
});
